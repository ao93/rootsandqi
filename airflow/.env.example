# Required by the official Airflow docker-compose setup to avoid file
# permission issues between the container and your host filesystem.
#
# Find your UID by running: id -u
# Copy this file to .env (in this airflow/ directory) and set the value:
AIRFLOW_UID=50000
